# {{projectName}}

{{description}}

A database MCP server built with [MCP Forge](https://github.com/user/mcp-forge).

## Quick Start

```bash
npm install && npm run build && npm start
```

## Configuration

Set `DB_PATH` environment variable to specify the database file path (defaults to `./data.db`).
