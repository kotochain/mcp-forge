# MCP Forge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a CLI tool that lets developers create production-ready MCP Servers in 30 seconds.

**Architecture:** Monorepo with pnpm workspaces. Four packages: `cli` (Commander.js CLI entry), `core` (project generation + template engine + config), `dev-server` (hot-reload dev mode with TUI), `templates` (built-in Handlebars templates). Templates generate TypeScript projects using `@modelcontextprotocol/sdk` McpServer API.

**Tech Stack:** TypeScript, Node.js 18+, Commander.js, Ink, Zod, Handlebars, @modelcontextprotocol/sdk, Vitest, tsup, pnpm

---

## File Structure

```
mcp-forge/
├── package.json                    # Root monorepo config
├── pnpm-workspace.yaml             # Workspace definition
├── tsconfig.base.json              # Shared TS config
├── packages/
│   ├── core/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── src/
│   │   │   ├── index.ts            # Public API exports
│   │   │   ├── generator.ts        # Project generation logic
│   │   │   ├── config.ts           # defineConfig + config loading
│   │   │   ├── schema.ts           # Zod schemas for tool definitions
│   │   │   └── template-engine.ts  # Handlebars template rendering
│   │   └── tests/
│   │       ├── generator.test.ts
│   │       ├── config.test.ts
│   │       └── schema.test.ts
│   ├── cli/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── src/
│   │   │   ├── index.ts            # CLI entry point
│   │   │   ├── commands/
│   │   │   │   ├── create.ts       # mcp-forge create
│   │   │   │   ├── dev.ts          # mcp-forge dev
│   │   │   │   ├── test.ts         # mcp-forge test
│   │   │   │   ├── inspect.ts      # mcp-forge inspect
│   │   │   │   └── add.ts          # mcp-forge add
│   │   │   └── prompts/
│   │   │       ├── create-prompts.ts   # Interactive prompts for create
│   │   │       └── add-prompts.ts      # Interactive prompts for add
│   │   └── tests/
│   │       └── commands/
│   │           └── create.test.ts
│   ├── dev-server/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── src/
│   │   │   ├── index.ts            # Public API
│   │   │   ├── watcher.ts          # File watcher
│   │   │   ├── runner.ts           # Server process runner
│   │   │   └── tui/
│   │   │       ├── index.ts        # TUI entry
│   │   │       ├── app.tsx         # Ink React app
│   │   │       ├── log-panel.tsx   # Log display
│   │   │       └── test-client.tsx # Interactive tool tester
│   │   └── tests/
│   │       └── watcher.test.ts
│   └── templates/
│       ├── package.json
│       ├── src/
│       │   ├── index.ts            # Template registry + loader
│       │   └── types.ts            # Template type definitions
│       └── templates/
│           ├── minimal/
│           │   ├── template.json
│           │   ├── package.json.hbs
│           │   ├── tsconfig.json.hbs
│           │   ├── src/
│           │   │   └── index.ts.hbs
│           │   └── README.md.hbs
│           ├── rest-api/
│           │   ├── template.json
│           │   ├── package.json.hbs
│           │   ├── src/
│           │   │   ├── index.ts.hbs
│           │   │   └── tools/
│           │   │       └── _tool.ts.hbs
│           │   └── README.md.hbs
│           ├── database/
│           │   ├── template.json
│           │   ├── package.json.hbs
│           │   ├── src/
│           │   │   ├── index.ts.hbs
│           │   │   └── tools/
│           │   │       └── _tool.ts.hbs
│           │   └── README.md.hbs
│           ├── filesystem/
│           │   ├── template.json
│           │   ├── package.json.hbs
│           │   ├── src/
│           │   │   ├── index.ts.hbs
│           │   │   └── tools/
│           │   │       └── _tool.ts.hbs
│           │   └── README.md.hbs
│           ├── web-scraper/
│           │   ├── template.json
│           │   ├── package.json.hbs
│           │   ├── src/
│           │   │   ├── index.ts.hbs
│           │   │   └── tools/
│           │   │       └── _tool.ts.hbs
│           │   └── README.md.hbs
│           └── ai-tool/
│               ├── template.json
│               ├── package.json.hbs
│               ├── src/
│               │   ├── index.ts.hbs
│               │   └── tools/
│               │       └── _tool.ts.hbs
│               └── README.md.hbs
└── examples/
    └── weather-server/             # Example generated project
```

---

### Task 1: Initialize Monorepo

**Files:**
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.base.json`
- Create: `.gitignore`

- [ ] **Step 1: Create root package.json**

```json
{
  "name": "mcp-forge",
  "version": "0.0.0",
  "private": true,
  "description": "The fastest way to build MCP Servers",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
```

- [ ] **Step 2: Create pnpm-workspace.yaml**

```yaml
packages:
  - 'packages/*'
```

- [ ] **Step 3: Create tsconfig.base.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": "./src"
  }
}
```

- [ ] **Step 4: Create .gitignore**

```
node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
coverage/
```

- [ ] **Step 5: Initialize git repo and commit**

Run: `cd /Users/javian/Downloads/trae && git init && git add -A && git commit -m "chore: initialize monorepo structure"`

---

### Task 2: Build Core Package - Schema & Config

**Files:**
- Create: `packages/core/package.json`
- Create: `packages/core/tsconfig.json`
- Create: `packages/core/src/index.ts`
- Create: `packages/core/src/schema.ts`
- Create: `packages/core/src/config.ts`
- Create: `packages/core/tests/schema.test.ts`
- Create: `packages/core/tests/config.test.ts`

- [ ] **Step 1: Create packages/core/package.json**

```json
{
  "name": "@mcp-forge/core",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: Create packages/core/tsconfig.json**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create packages/core/tsup.config.ts**

```typescript
import { defineConfig } from 'tsup';

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: true,
  clean: true,
  sourcemap: true,
});
```

- [ ] **Step 4: Write the failing test for schema**

Create `packages/core/tests/schema.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { ToolDefinitionSchema, ProjectConfigSchema } from '../src/schema.js';

describe('ToolDefinitionSchema', () => {
  it('validates a minimal tool definition', () => {
    const tool = {
      name: 'search',
      description: 'Search for items',
      parameters: {
        type: 'object' as const,
        properties: {
          query: { type: 'string' as const, description: 'Search query' },
        },
        required: ['query'],
      },
    };
    const result = ToolDefinitionSchema.safeParse(tool);
    expect(result.success).toBe(true);
  });

  it('rejects tool without name', () => {
    const tool = {
      description: 'Search for items',
      parameters: { type: 'object' as const, properties: {} },
    };
    const result = ToolDefinitionSchema.safeParse(tool);
    expect(result.success).toBe(false);
  });
});

describe('ProjectConfigSchema', () => {
  it('validates a complete project config', () => {
    const config = {
      server: { name: 'my-server', version: '1.0.0' },
      tools: ['./src/tools/*.ts'],
      dev: { watch: ['./src'], hotReload: true },
    };
    const result = ProjectConfigSchema.safeParse(config);
    expect(result.success).toBe(true);
  });

  it('provides defaults for optional fields', () => {
    const config = {
      server: { name: 'my-server', version: '1.0.0' },
    };
    const result = ProjectConfigSchema.safeParse(config);
    expect(result.success).toBe(true);
    if (result.success) {
      expect(result.data.tools).toEqual(['./src/tools/*.ts']);
      expect(result.data.dev?.hotReload).toBe(true);
    }
  });
});
```

- [ ] **Step 5: Run test to verify it fails**

Run: `cd /Users/javian/Downloads/trae/packages/core && pnpm install && pnpm test`
Expected: FAIL - modules not found

- [ ] **Step 6: Implement schema.ts**

Create `packages/core/src/schema.ts`:

```typescript
import { z } from 'zod/v3';

export const ToolParameterSchema = z.object({
  type: z.enum(['string', 'number', 'boolean', 'array', 'object']),
  description: z.string().optional(),
  items: z.lazy(() => ToolParameterSchema).optional(),
  properties: z.record(z.lazy(() => ToolParameterSchema)).optional(),
  required: z.array(z.string()).optional(),
  default: z.unknown().optional(),
});

export const ToolDefinitionSchema = z.object({
  name: z.string().min(1, 'Tool name is required'),
  description: z.string().min(1, 'Tool description is required'),
  parameters: z.object({
    type: z.literal('object'),
    properties: z.record(ToolParameterSchema).default({}),
    required: z.array(z.string()).default([]),
  }).default({ type: 'object', properties: {}, required: [] }),
});

export type ToolDefinition = z.infer<typeof ToolDefinitionSchema>;
export type ToolParameter = z.infer<typeof ToolParameterSchema>;

export const ProjectConfigSchema = z.object({
  server: z.object({
    name: z.string().min(1),
    version: z.string().default('1.0.0'),
    description: z.string().optional(),
  }),
  tools: z.array(z.string()).default(['./src/tools/*.ts']),
  dev: z.object({
    watch: z.array(z.string()).default(['./src']),
    hotReload: z.boolean().default(true),
  }).default({}),
  transport: z.enum(['stdio', 'streamable-http']).default('stdio'),
});

export type ProjectConfig = z.infer<typeof ProjectConfigSchema>;
```

- [ ] **Step 7: Implement config.ts**

Create `packages/core/src/config.ts`:

```typescript
import { ProjectConfigSchema, type ProjectConfig } from './schema.js';
import { readFile } from 'node:fs/promises';
import { resolve } from 'node:path';

export function defineConfig(config: ProjectConfig): ProjectConfig {
  return ProjectConfigSchema.parse(config);
}

export async function loadConfig(projectPath: string): Promise<ProjectConfig> {
  const configPath = resolve(projectPath, 'mcp-forge.config.ts');

  try {
    const content = await readFile(configPath, 'utf-8');
    // Dynamic import for .ts config files (requires tsx or ts-node at runtime)
    const moduleUrl = `file://${configPath}`;
    const module = await import(moduleUrl);
    const config = module.default || module;
    return ProjectConfigSchema.parse(config);
  } catch (error) {
    throw new Error(
      `Failed to load config from ${configPath}: ${error instanceof Error ? error.message : String(error)}`
    );
  }
}

export function createDefaultConfig(name: string, description?: string): ProjectConfig {
  return ProjectConfigSchema.parse({
    server: { name, description },
  });
}
```

- [ ] **Step 8: Create index.ts barrel export**

Create `packages/core/src/index.ts`:

```typescript
export { defineConfig, loadConfig, createDefaultConfig } from './config.js';
export { ToolDefinitionSchema, ProjectConfigSchema } from './schema.js';
export type { ToolDefinition, ToolParameter, ProjectConfig } from './schema.js';
export { generateProject } from './generator.js';
export { renderTemplate } from './template-engine.js';
```

- [ ] **Step 9: Run tests to verify they pass**

Run: `cd /Users/javian/Downloads/trae/packages/core && pnpm test`
Expected: PASS

- [ ] **Step 10: Commit**

```bash
cd /Users/javian/Downloads/trae && git add packages/core && git commit -m "feat(core): add schema validation and config loading"
```

---

### Task 3: Build Core Package - Template Engine & Generator

**Files:**
- Create: `packages/core/src/template-engine.ts`
- Create: `packages/core/src/generator.ts`
- Create: `packages/core/tests/template-engine.test.ts`
- Create: `packages/core/tests/generator.test.ts`

- [ ] **Step 1: Write the failing test for template engine**

Create `packages/core/tests/template-engine.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { renderTemplate, registerHelpers } from '../src/template-engine.js';

describe('renderTemplate', () => {
  it('renders a simple template with variables', () => {
    const template = 'Hello {{name}}!';
    const result = renderTemplate(template, { name: 'World' });
    expect(result).toBe('Hello World!');
  });

  it('renders tool definitions in a loop', () => {
    const template = '{{#each tools}}  {{name}}: {{description}}\n{{/each}}';
    const result = renderTemplate(template, {
      tools: [
        { name: 'search', description: 'Search items' },
        { name: 'create', description: 'Create items' },
      ],
    });
    expect(result).toContain('search: Search items');
    expect(result).toContain('create: Create items');
  });

  it('converts camelCase to kebab-case via helper', () => {
    const template = '{{kebabCase name}}';
    const result = renderTemplate(template, { name: 'myCoolTool' });
    expect(result).toBe('my-cool-tool');
  });

  it('converts to PascalCase via helper', () => {
    const template = '{{pascalCase name}}';
    const result = renderTemplate(template, { name: 'my-cool-tool' });
    expect(result).toBe('MyCoolTool');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/javian/Downloads/trae/packages/core && pnpm test`
Expected: FAIL - module not found

- [ ] **Step 3: Add handlebars dependency**

Run: `cd /Users/javian/Downloads/trae/packages/core && pnpm add handlebars && pnpm add -D @types/handlebars`

- [ ] **Step 4: Implement template-engine.ts**

Create `packages/core/src/template-engine.ts`:

```typescript
import Handlebars from 'handlebars';

export function registerHelpers(): void {
  Handlebars.registerHelper('kebabCase', (str: string) => {
    return str
      .replace(/([a-z])([A-Z])/g, '$1-$2')
      .replace(/[\s_]+/g, '-')
      .toLowerCase();
  });

  Handlebars.registerHelper('pascalCase', (str: string) => {
    return str
      .replace(/(^|[-_\s])(\w)/g, (_match, _sep, char) => char.toUpperCase())
      .replace(/[-_\s]/g, '');
  });

  Handlebars.registerHelper('camelCase', (str: string) => {
    const pascal = str
      .replace(/(^|[-_\s])(\w)/g, (_match: string, _sep: string, char: string) => char.toUpperCase())
      .replace(/[-_\s]/g, '');
    return pascal.charAt(0).toLowerCase() + pascal.slice(1);
  });

  Handlebars.registerHelper('json', (obj: unknown) => {
    return new Handlebars.SafeString(JSON.stringify(obj, null, 2));
  });
}

// Register helpers on module load
registerHelpers();

export function renderTemplate(template: string, data: Record<string, unknown>): string {
  const compiled = Handlebars.compile(template);
  return compiled(data);
}
```

- [ ] **Step 5: Write the failing test for generator**

Create `packages/core/tests/generator.test.ts`:

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { generateProject } from '../src/generator.js';
import { mkdir, rm, stat } from 'node:fs/promises';
import { join } from 'node:path';
import { readFile } from 'node:fs/promises';

const TEST_DIR = join(import.meta.dirname, '__test_output__');

describe('generateProject', () => {
  beforeEach(async () => {
    await mkdir(TEST_DIR, { recursive: true });
  });

  afterEach(async () => {
    await rm(TEST_DIR, { recursive: true, force: true });
  });

  it('generates a minimal project with correct files', async () => {
    const projectPath = join(TEST_DIR, 'test-project');
    await generateProject({
      projectName: 'test-project',
      description: 'A test project',
      author: 'Test Author',
      template: 'minimal',
      tools: [],
      outputPath: projectPath,
    });

    // Check that key files exist
    const packageJson = JSON.parse(
      await readFile(join(projectPath, 'package.json'), 'utf-8')
    );
    expect(packageJson.name).toBe('test-project');
    expect(packageJson.dependencies['@modelcontextprotocol/sdk']).toBeDefined();

    const indexTs = await readFile(join(projectPath, 'src', 'index.ts'), 'utf-8');
    expect(indexTs).toContain('McpServer');
  });

  it('generates a project with tools', async () => {
    const projectPath = join(TEST_DIR, 'tool-project');
    await generateProject({
      projectName: 'tool-project',
      description: 'A project with tools',
      author: 'Test Author',
      template: 'rest-api',
      tools: [
        {
          name: 'search',
          description: 'Search for items',
          parameters: {
            type: 'object',
            properties: {
              query: { type: 'string', description: 'Search query' },
            },
            required: ['query'],
          },
        },
      ],
      outputPath: projectPath,
    });

    const indexTs = await readFile(join(projectPath, 'src', 'index.ts'), 'utf-8');
    expect(indexTs).toContain('search');

    const toolFile = await readFile(join(projectPath, 'src', 'tools', 'search.ts'), 'utf-8');
    expect(toolFile).toContain('search');
    expect(toolFile).toContain('query');
  });
});
```

- [ ] **Step 6: Implement generator.ts**

Create `packages/core/src/generator.ts`:

```typescript
import { mkdir, writeFile, copyFile } from 'node:fs/promises';
import { join, dirname } from 'node:path';
import { renderTemplate } from './template-engine.js';
import type { ToolDefinition, ProjectConfig } from './schema.js';
import { ProjectConfigSchema } from './schema.js';

export interface GenerateProjectOptions {
  projectName: string;
  description: string;
  author: string;
  template: string;
  tools: ToolDefinition[];
  outputPath: string;
}

interface TemplateFile {
  path: string;        // Relative output path (may contain .hbs extension to strip)
  content: string;     // Template content or raw content
  isTemplate: boolean; // Whether to run through Handlebars
}

async function ensureDir(filePath: string): Promise<void> {
  await mkdir(dirname(filePath), { recursive: true });
}

export async function generateProject(options: GenerateProjectOptions): Promise<void> {
  const { projectName, description, author, template, tools, outputPath } = options;

  // Resolve template directory
  const templateDir = await resolveTemplateDir(template);

  // Load template manifest
  const manifest = await loadTemplateManifest(templateDir);

  // Build template data context
  const templateData = {
    projectName,
    description,
    author,
    tools,
    serverName: projectName,
    version: '1.0.0',
    hasTools: tools.length > 0,
    transport: 'stdio',
  };

  // Generate each file from the template
  for (const file of manifest.files) {
    const targetPath = join(outputPath, file.path.replace('.hbs', ''));
    await ensureDir(targetPath);

    let content: string;
    if (file.isTemplate) {
      content = renderTemplate(file.content, templateData);
    } else {
      content = file.content;
    }

    await writeFile(targetPath, content, 'utf-8');
  }

  // Generate tool files if tools are defined
  if (tools.length > 0) {
    await generateToolFiles(outputPath, tools, templateDir);
  }

  // Generate config file
  await generateConfigFile(outputPath, projectName, description);
}

async function resolveTemplateDir(templateName: string): Promise<string> {
  // First check built-in templates
  const builtInPath = join(
    dirname(new URL(import.meta.url).pathname),
    '../../templates/templates',
    templateName
  );

  try {
    await stat(builtInPath);
    return builtInPath;
  } catch {
    throw new Error(`Template "${templateName}" not found`);
  }
}

async function stat(path: string): Promise<void> {
  const { stat: statFn } = await import('node:fs/promises');
  await statFn(path);
}

interface TemplateManifest {
  name: string;
  description: string;
  files: TemplateFile[];
}

async function loadTemplateManifest(templateDir: string): Promise<TemplateManifest> {
  const { readFile } = await import('node:fs/promises');
  const { readdir } = await import('node:fs/promises');

  const manifestPath = join(templateDir, 'template.json');
  const manifestContent = await readFile(manifestPath, 'utf-8');
  const manifest = JSON.parse(manifestContent);

  // Read all template files
  const files: TemplateFile[] = [];

  async function walkTemplateDir(dir: string, relativePath: string): Promise<void> {
    const entries = await readdir(dir, { withFileTypes: true });

    for (const entry of entries) {
      const fullPath = join(dir, entry.name);
      const relPath = join(relativePath, entry.name);

      if (entry.name === 'template.json') continue;

      if (entry.isDirectory()) {
        await walkTemplateDir(fullPath, relPath);
      } else {
        const content = await readFile(fullPath, 'utf-8');
        const isTemplate = entry.name.endsWith('.hbs');
        files.push({ path: relPath, content, isTemplate });
      }
    }
  }

  await walkTemplateDir(templateDir, '');

  return { ...manifest, files };
}

async function generateToolFiles(
  outputPath: string,
  tools: ToolDefinition[],
  templateDir: string
): Promise<void> {
  const { readFile } = await import('node:fs/promises');

  // Try to find a tool template in the template directory
  let toolTemplate: string;
  try {
    toolTemplate = await readFile(join(templateDir, 'src', 'tools', '_tool.ts.hbs'), 'utf-8');
  } catch {
    // Fallback tool template
    toolTemplate = `import { z } from 'zod/v3';
import {{projectName}} from '../index.js';

export function register{{pascalCase name}}(server: any) {
  server.tool(
    '{{name}}',
    '{{description}}',
    {
      {{#each parameters.properties}}
      {{@key}}: z.{{jsType this}}(){{#if description}}, // {{description}}{{/if}},
      {{/each}}
    },
    async ({ {{parameterList}} }) => {
      // TODO: Implement {{name}} tool logic
      return {
        content: [{ type: 'text', text: JSON.stringify({ result: '{{name}} executed' }) }],
      };
    }
  );
}
`;
  }

  for (const tool of tools) {
    const paramNames = Object.keys(tool.parameters.properties || {});
    const templateData = {
      ...tool,
      projectName: '',
      parameterList: paramNames.join(', '),
      parameters: {
        ...tool.parameters,
        properties: Object.fromEntries(
          Object.entries(tool.parameters.properties || {}).map(([key, val]) => [
            key,
            { ...val, jsType: zodTypeFromSchema(val as { type: string }) },
          ])
        ),
      },
    };

    const content = renderTemplate(toolTemplate, templateData);
    const toolPath = join(outputPath, 'src', 'tools', `${tool.name}.ts`);
    await ensureDir(toolPath);
    await writeFile(toolPath, content, 'utf-8');
  }
}

function zodTypeFromSchema(param: { type: string }): string {
  const typeMap: Record<string, string> = {
    string: 'string',
    number: 'number',
    boolean: 'boolean',
    array: 'array',
    object: 'object',
  };
  return typeMap[param.type] || 'string';
}

async function generateConfigFile(
  outputPath: string,
  projectName: string,
  description: string
): Promise<void> {
  const configContent = `import { defineConfig } from '@mcp-forge/core';

export default defineConfig({
  server: {
    name: '${projectName}',
    version: '1.0.0',
    ${description ? `description: '${description}',` : ''}
  },
  tools: ['./src/tools/*.ts'],
  dev: {
    watch: ['./src'],
    hotReload: true,
  },
});
`;

  const configPath = join(outputPath, 'mcp-forge.config.ts');
  await writeFile(configPath, configContent, 'utf-8');
}
```

- [ ] **Step 7: Run tests**

Run: `cd /Users/javian/Downloads/trae/packages/core && pnpm test`
Expected: Template engine tests PASS; generator tests need templates (will pass after Task 4)

- [ ] **Step 8: Commit**

```bash
cd /Users/javian/Downloads/trae && git add packages/core && git commit -m "feat(core): add template engine and project generator"
```

---

### Task 4: Build Templates Package

**Files:**
- Create: `packages/templates/package.json`
- Create: `packages/templates/tsconfig.json`
- Create: `packages/templates/src/index.ts`
- Create: `packages/templates/src/types.ts`
- Create: `packages/templates/templates/minimal/template.json`
- Create: `packages/templates/templates/minimal/package.json.hbs`
- Create: `packages/templates/templates/minimal/tsconfig.json` (static)
- Create: `packages/templates/templates/minimal/src/index.ts.hbs`
- Create: `packages/templates/templates/minimal/README.md.hbs`
- Create: `packages/templates/templates/rest-api/template.json`
- Create: `packages/templates/templates/rest-api/package.json.hbs`
- Create: `packages/templates/templates/rest-api/src/index.ts.hbs`
- Create: `packages/templates/templates/rest-api/src/tools/_tool.ts.hbs`
- Create: `packages/templates/templates/rest-api/README.md.hbs`
- Create: `packages/templates/templates/database/template.json`
- Create: `packages/templates/templates/database/package.json.hbs`
- Create: `packages/templates/templates/database/src/index.ts.hbs`
- Create: `packages/templates/templates/database/src/tools/_tool.ts.hbs`
- Create: `packages/templates/templates/database/README.md.hbs`
- Create: `packages/templates/templates/filesystem/template.json`
- Create: `packages/templates/templates/filesystem/package.json.hbs`
- Create: `packages/templates/templates/filesystem/src/index.ts.hbs`
- Create: `packages/templates/templates/filesystem/src/tools/_tool.ts.hbs`
- Create: `packages/templates/templates/filesystem/README.md.hbs`
- Create: `packages/templates/templates/web-scraper/template.json`
- Create: `packages/templates/templates/web-scraper/package.json.hbs`
- Create: `packages/templates/templates/web-scraper/src/index.ts.hbs`
- Create: `packages/templates/templates/web-scraper/src/tools/_tool.ts.hbs`
- Create: `packages/templates/templates/web-scraper/README.md.hbs`
- Create: `packages/templates/templates/ai-tool/template.json`
- Create: `packages/templates/templates/ai-tool/package.json.hbs`
- Create: `packages/templates/templates/ai-tool/src/index.ts.hbs`
- Create: `packages/templates/templates/ai-tool/src/tools/_tool.ts.hbs`
- Create: `packages/templates/templates/ai-tool/README.md.hbs`

- [ ] **Step 1: Create packages/templates/package.json**

```json
{
  "name": "@mcp-forge/templates",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: Create types.ts**

Create `packages/templates/src/types.ts`:

```typescript
export interface TemplateManifest {
  name: string;
  description: string;
  category: 'minimal' | 'api' | 'data' | 'web' | 'ai';
  dependencies?: Record<string, string>;
  prompts?: TemplatePrompt[];
}

export interface TemplatePrompt {
  name: string;
  message: string;
  type: 'text' | 'select' | 'confirm';
  default?: string | boolean;
  choices?: { name: string; value: string }[];
}

export interface TemplateInfo {
  name: string;
  description: string;
  category: string;
  path: string;
}
```

- [ ] **Step 3: Create index.ts (template registry)**

Create `packages/templates/src/index.ts`:

```typescript
import { readdir, readFile, stat } from 'node:fs/promises';
import { join, dirname } from 'node:path';
import type { TemplateInfo, TemplateManifest } from './types.js';

export type { TemplateInfo, TemplateManifest } from './types.js';

const TEMPLATES_DIR = join(dirname(new URL(import.meta.url).pathname), '..', 'templates');

export async function listTemplates(): Promise<TemplateInfo[]> {
  const entries = await readdir(TEMPLATES_DIR, { withFileTypes: true });
  const templates: TemplateInfo[] = [];

  for (const entry of entries) {
    if (!entry.isDirectory()) continue;

    try {
      const manifestPath = join(TEMPLATES_DIR, entry.name, 'template.json');
      const content = await readFile(manifestPath, 'utf-8');
      const manifest: TemplateManifest = JSON.parse(content);

      templates.push({
        name: manifest.name,
        description: manifest.description,
        category: manifest.category,
        path: join(TEMPLATES_DIR, entry.name),
      });
    } catch {
      // Skip invalid templates
    }
  }

  return templates;
}

export async function getTemplate(name: string): Promise<TemplateInfo | null> {
  const templates = await listTemplates();
  return templates.find((t) => t.name === name) ?? null;
}

export function getTemplatesDir(): string {
  return TEMPLATES_DIR;
}
```

- [ ] **Step 4: Create minimal template files**

Create `packages/templates/templates/minimal/template.json`:

```json
{
  "name": "minimal",
  "description": "A minimal MCP server with a single echo tool",
  "category": "minimal"
}
```

Create `packages/templates/templates/minimal/package.json.hbs`:

```handlebars
{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
```

Create `packages/templates/templates/minimal/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "declaration": true
  },
  "include": ["src"]
}
```

Create `packages/templates/templates/minimal/src/index.ts.hbs`:

```handlebars
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';

const server = new McpServer({
  name: '{{projectName}}',
  version: '1.0.0',
});

server.tool(
  'echo',
  'Echo back the input message',
  { message: z.string().describe('Message to echo') },
  async ({ message }) => {
    return {
      content: [{ type: 'text', text: `Echo: ${message}` }],
    };
  }
);

{{#each tools}}
// Tool: {{name}} - {{description}}
// Register this tool by importing from ./tools/{{name}}.ts
{{/each}}

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch(console.error);
```

Create `packages/templates/templates/minimal/README.md.hbs`:

```handlebars
# {{projectName}}

{{description}}

Built with [MCP Forge](https://github.com/user/mcp-forge).

## Quick Start

```bash
# Install dependencies
npm install

# Build
npm run build

# Run
npm start

# Inspect with MCP Inspector
npm run inspect
```

## Usage with Claude Desktop

Add this to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "{{projectName}}": {
      "command": "node",
      "args": ["/path/to/{{projectName}}/dist/index.js"]
    }
  }
}
```
```

- [ ] **Step 5: Create rest-api template files**

Create `packages/templates/templates/rest-api/template.json`:

```json
{
  "name": "rest-api",
  "description": "MCP server that wraps a REST API with tool calls",
  "category": "api",
  "dependencies": {
    "node-fetch": "^3.0.0"
  }
}
```

Create `packages/templates/templates/rest-api/package.json.hbs`:

```handlebars
{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
```

Create `packages/templates/templates/rest-api/src/index.ts.hbs`:

```handlebars
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
{{#each tools}}
import { register{{pascalCase name}} } from './tools/{{name}}.js';
{{/each}}

const server = new McpServer({
  name: '{{projectName}}',
  version: '1.0.0',
});

{{#each tools}}
register{{pascalCase name}}(server);
{{/each}}

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch(console.error);
```

Create `packages/templates/templates/rest-api/src/tools/_tool.ts.hbs`:

```handlebars
import { z } from 'zod/v3';

export function register{{pascalCase name}}(server: import('@modelcontextprotocol/sdk/server/mcp.js').McpServer) {
  server.tool(
    '{{name}}',
    '{{description}}',
    {
      {{#each parameters.properties}}
      {{@key}}: z.{{zodType this}}(){{#if this.description}}.describe('{{this.description}}'){{/if}},
      {{/each}}
    },
    async ({ {{parameterList}} }) => {
      // TODO: Implement {{name}} - call your REST API here
      const response = await fetch('https://api.example.com/{{name}}', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ {{parameterList}} }),
      });

      const data = await response.json();
      return {
        content: [{ type: 'text', text: JSON.stringify(data, null, 2) }],
      };
    }
  );
}
```

Create `packages/templates/templates/rest-api/README.md.hbs`:

```handlebars
# {{projectName}}

{{description}}

A REST API wrapper MCP server built with [MCP Forge](https://github.com/user/mcp-forge).

## Quick Start

```bash
npm install && npm run build && npm start
```

## Tools

{{#each tools}}
- **{{name}}**: {{description}}
{{/each}}

## Usage with Claude Desktop

Add to `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "{{projectName}}": {
      "command": "node",
      "args": ["/path/to/{{projectName}}/dist/index.js"]
    }
  }
}
```
```

- [ ] **Step 6: Create database template files**

Create `packages/templates/templates/database/template.json`:

```json
{
  "name": "database",
  "description": "MCP server for database operations (query, insert, update, delete)",
  "category": "data"
}
```

Create `packages/templates/templates/database/package.json.hbs`:

```handlebars
{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "@types/better-sqlite3": "^7.6.0"
  }
}
```

Create `packages/templates/templates/database/src/index.ts.hbs`:

```handlebars
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import Database from 'better-sqlite3';
import path from 'node:path';
{{#each tools}}
import { register{{pascalCase name}} } from './tools/{{name}}.js';
{{/each}}

const dbPath = process.env.DB_PATH || path.join(process.cwd(), 'data.db');
const db = new Database(dbPath);

// Enable WAL mode for better concurrent read performance
db.pragma('journal_mode = WAL');

const server = new McpServer({
  name: '{{projectName}}',
  version: '1.0.0',
});

{{#each tools}}
register{{pascalCase name}}(server, db);
{{/each}}

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch(console.error);
```

Create `packages/templates/templates/database/src/tools/_tool.ts.hbs`:

```handlebars
import { z } from 'zod/v3';
import type Database from 'better-sqlite3';

export function register{{pascalCase name}}(server: import('@modelcontextprotocol/sdk/server/mcp.js').McpServer, db: Database.Database) {
  server.tool(
    '{{name}}',
    '{{description}}',
    {
      {{#each parameters.properties}}
      {{@key}}: z.{{zodType this}}(){{#if this.description}}.describe('{{this.description}}'){{/if}},
      {{/each}}
    },
    async ({ {{parameterList}} }) => {
      // TODO: Implement {{name}} database operation
      try {
        const result = db.prepare('SELECT 1 as test').all();
        return {
          content: [{ type: 'text', text: JSON.stringify(result, null, 2) }],
        };
      } catch (error) {
        return {
          content: [{ type: 'text', text: `Error: ${error instanceof Error ? error.message : String(error)}` }],
          isError: true,
        };
      }
    }
  );
}
```

Create `packages/templates/templates/database/README.md.hbs`:

```handlebars
# {{projectName}}

{{description}}

A database MCP server built with [MCP Forge](https://github.com/user/mcp-forge).

## Quick Start

```bash
npm install && npm run build && npm start
```

## Configuration

Set the `DB_PATH` environment variable to specify the database file path (defaults to `./data.db`).

## Tools

{{#each tools}}
- **{{name}}**: {{description}}
{{/each}}
```

- [ ] **Step 7: Create filesystem template files**

Create `packages/templates/templates/filesystem/template.json`:

```json
{
  "name": "filesystem",
  "description": "MCP server for file system operations (read, write, list, search)",
  "category": "data"
}
```

Create `packages/templates/templates/filesystem/package.json.hbs`:

```handlebars
{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0",
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
```

Create `packages/templates/templates/filesystem/src/index.ts.hbs`:

```handlebars
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
{{#each tools}}
import { register{{pascalCase name}} } from './tools/{{name}}.js';
{{/each}}

const allowedDirs = (process.env.ALLOWED_DIRS || process.cwd()).split(',');

const server = new McpServer({
  name: '{{projectName}}',
  version: '1.0.0',
});

{{#each tools}}
register{{pascalCase name}}(server, allowedDirs);
{{/each}}

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch(console.error);
```

Create `packages/templates/templates/filesystem/src/tools/_tool.ts.hbs`:

```handlebars
import { z } from 'zod/v3';

export function register{{pascalCase name}}(server: import('@modelcontextprotocol/sdk/server/mcp.js').McpServer, allowedDirs: string[]) {
  server.tool(
    '{{name}}',
    '{{description}}',
    {
      {{#each parameters.properties}}
      {{@key}}: z.{{zodType this}}(){{#if this.description}}.describe('{{this.description}}'){{/if}},
      {{/each}}
    },
    async ({ {{parameterList}} }) => {
      // TODO: Implement {{name}} file system operation
      // Validate path is within allowedDirs
      return {
        content: [{ type: 'text', text: '{{name}} executed' }],
      };
    }
  );
}
```

Create `packages/templates/templates/filesystem/README.md.hbs`:

```handlebars
# {{projectName}}

{{description}}

A filesystem MCP server built with [MCP Forge](https://github.com/user/mcp-forge).

## Quick Start

```bash
npm install && npm run build && npm start
```

## Configuration

Set `ALLOWED_DIRS` to comma-separated list of allowed directories (defaults to current directory).

## Tools

{{#each tools}}
- **{{name}}**: {{description}}
{{/each}}
```

- [ ] **Step 8: Create web-scraper template files**

Create `packages/templates/templates/web-scraper/template.json`:

```json
{
  "name": "web-scraper",
  "description": "MCP server for web scraping and content extraction",
  "category": "web"
}
```

Create `packages/templates/templates/web-scraper/package.json.hbs`:

```handlebars
{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
```

Create `packages/templates/templates/web-scraper/src/index.ts.hbs`:

```handlebars
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
{{#each tools}}
import { register{{pascalCase name}} } from './tools/{{name}}.js';
{{/each}}

const server = new McpServer({
  name: '{{projectName}}',
  version: '1.0.0',
});

{{#each tools}}
register{{pascalCase name}}(server);
{{/each}}

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch(console.error);
```

Create `packages/templates/templates/web-scraper/src/tools/_tool.ts.hbs`:

```handlebars
import { z } from 'zod/v3';
import * as cheerio from 'cheerio';

export function register{{pascalCase name}}(server: import('@modelcontextprotocol/sdk/server/mcp.js').McpServer) {
  server.tool(
    '{{name}}',
    '{{description}}',
    {
      {{#each parameters.properties}}
      {{@key}}: z.{{zodType this}}(){{#if this.description}}.describe('{{this.description}}'){{/if}},
      {{/each}}
    },
    async ({ {{parameterList}} }) => {
      // TODO: Implement {{name}} web scraping operation
      try {
        const response = await fetch(url);
        const html = await response.text();
        const $ = cheerio.load(html);
        return {
          content: [{ type: 'text', text: $('body').text().trim().slice(0, 5000) }],
        };
      } catch (error) {
        return {
          content: [{ type: 'text', text: `Error: ${error instanceof Error ? error.message : String(error)}` }],
          isError: true,
        };
      }
    }
  );
}
```

Create `packages/templates/templates/web-scraper/README.md.hbs`:

```handlebars
# {{projectName}}

{{description}}

A web scraper MCP server built with [MCP Forge](https://github.com/user/mcp-forge).

## Quick Start

```bash
npm install && npm run build && npm start
```

## Tools

{{#each tools}}
- **{{name}}**: {{description}}
{{/each}}
```

- [ ] **Step 9: Create ai-tool template files**

Create `packages/templates/templates/ai-tool/template.json`:

```json
{
  "name": "ai-tool",
  "description": "MCP server that wraps AI/LLM API calls as tools",
  "category": "ai"
}
```

Create `packages/templates/templates/ai-tool/package.json.hbs`:

```handlebars
{
  "name": "{{projectName}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0",
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
```

Create `packages/templates/templates/ai-tool/src/index.ts.hbs`:

```handlebars
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { StdioServerTransport } from '@modelcontextprotocol/sdk/server/stdio.js';
import OpenAI from 'openai';
{{#each tools}}
import { register{{pascalCase name}} } from './tools/{{name}}.js';
{{/each}}

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
});

const server = new McpServer({
  name: '{{projectName}}',
  version: '1.0.0',
});

{{#each tools}}
register{{pascalCase name}}(server, openai);
{{/each}}

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
}

main().catch(console.error);
```

Create `packages/templates/templates/ai-tool/src/tools/_tool.ts.hbs`:

```handlebars
import { z } from 'zod/v3';
import type OpenAI from 'openai';

export function register{{pascalCase name}}(server: import('@modelcontextprotocol/sdk/server/mcp.js').McpServer, openai: OpenAI) {
  server.tool(
    '{{name}}',
    '{{description}}',
    {
      {{#each parameters.properties}}
      {{@key}}: z.{{zodType this}}(){{#if this.description}}.describe('{{this.description}}'){{/if}},
      {{/each}}
    },
    async ({ {{parameterList}} }) => {
      // TODO: Implement {{name}} AI tool operation
      try {
        const completion = await openai.chat.completions.create({
          model: 'gpt-4o-mini',
          messages: [{ role: 'user', content: prompt }],
        });
        return {
          content: [{ type: 'text', text: completion.choices[0]?.message?.content || 'No response' }],
        };
      } catch (error) {
        return {
          content: [{ type: 'text', text: `Error: ${error instanceof Error ? error.message : String(error)}` }],
          isError: true,
        };
      }
    }
  );
}
```

Create `packages/templates/templates/ai-tool/README.md.hbs`:

```handlebars
# {{projectName}}

{{description}}

An AI tool MCP server built with [MCP Forge](https://github.com/user/mcp-forge).

## Quick Start

```bash
npm install && npm run build && npm start
```

## Configuration

Set `OPENAI_API_KEY` environment variable with your OpenAI API key.

## Tools

{{#each tools}}
- **{{name}}**: {{description}}
{{/each}}
```

- [ ] **Step 10: Commit**

```bash
cd /Users/javian/Downloads/trae && git add packages/templates && git commit -m "feat(templates): add all 6 built-in templates"
```

---

### Task 5: Build CLI Package - Create Command

**Files:**
- Create: `packages/cli/package.json`
- Create: `packages/cli/tsconfig.json`
- Create: `packages/cli/tsup.config.ts`
- Create: `packages/cli/src/index.ts`
- Create: `packages/cli/src/commands/create.ts`
- Create: `packages/cli/src/prompts/create-prompts.ts`

- [ ] **Step 1: Create packages/cli/package.json**

```json
{
  "name": "@mcp-forge/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "mcp-forge": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@mcp-forge/core": "workspace:*",
    "@mcp-forge/templates": "workspace:*",
    "commander": "^12.0.0",
    "picocolors": "^1.1.0",
    "clack-prompts": "^0.1.0",
    "execa": "^9.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: Create tsup.config.ts**

```typescript
import { defineConfig } from 'tsup';

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: false,
  clean: true,
  sourcemap: true,
  banner: {
    js: '#!/usr/bin/env node',
  },
});
```

- [ ] **Step 3: Implement create-prompts.ts**

Create `packages/cli/src/prompts/create-prompts.ts`:

```typescript
import * as clack from '@clack/prompts';
import type { ToolDefinition } from '@mcp-forge/core';
import { listTemplates } from '@mcp-forge/templates';

export interface CreateAnswers {
  projectName: string;
  description: string;
  author: string;
  template: string;
  tools: ToolDefinition[];
}

export async function runCreatePrompts(options: Partial<CreateAnswers> = {}): Promise<CreateAnswers | null> {
  clack.intro('Welcome to MCP Forge!');

  const templates = await listTemplates();

  const answers = await clack.group({
    projectName: () =>
      clack.text({
        message: 'Project name?',
        placeholder: 'my-mcp-server',
        defaultValue: 'my-mcp-server',
        validate: (v) => (!v.trim() ? 'Project name is required' : undefined),
      }),
    description: () =>
      clack.text({
        message: 'Description?',
        placeholder: 'My MCP Server',
      }),
    author: () =>
      clack.text({
        message: 'Author?',
        placeholder: 'Your Name',
      }),
    template: () =>
      clack.select({
        message: 'Choose a template',
        options: templates.map((t) => ({
          value: t.name,
          label: t.name,
          hint: t.description,
        })),
      }),
    addTools: () =>
      clack.confirm({
        message: 'Add custom tools now?',
        initialValue: true,
      }),
  });

  if (clack.isCancel(answers)) {
    clack.cancel('Operation cancelled');
    return null;
  }

  let tools: ToolDefinition[] = [];

  if (answers.addTools) {
    tools = await collectToolDefinitions();
  }

  clack.outro('Project ready!');

  return {
    projectName: options.projectName || (answers.projectName as string),
    description: options.description || (answers.description as string) || '',
    author: options.author || (answers.author as string) || '',
    template: options.template || (answers.template as string),
    tools,
  };
}

async function collectToolDefinitions(): Promise<ToolDefinition[]> {
  const tools: ToolDefinition[] = [];
  let addMore = true;

  while (addMore) {
    const toolAnswers = await clack.group({
      name: () =>
        clack.text({
          message: 'Tool name?',
          placeholder: 'search',
          validate: (v) => (!v.trim() ? 'Tool name is required' : undefined),
        }),
      description: () =>
        clack.text({
          message: 'Tool description?',
          placeholder: 'Search for items',
        }),
      addParams: () =>
        clack.confirm({
          message: 'Add parameters?',
          initialValue: true,
        }),
    });

    if (clack.isCancel(toolAnswers)) break;

    const parameters: ToolDefinition['parameters'] = {
      type: 'object',
      properties: {},
      required: [],
    };

    if (toolAnswers.addParams) {
      let addMoreParams = true;
      while (addMoreParams) {
        const paramAnswers = await clack.group({
          paramName: () =>
            clack.text({
              message: 'Parameter name?',
              placeholder: 'query',
            }),
          paramType: () =>
            clack.select({
              message: 'Parameter type?',
              options: [
                { value: 'string', label: 'string' },
                { value: 'number', label: 'number' },
                { value: 'boolean', label: 'boolean' },
                { value: 'array', label: 'array' },
              ],
            }),
          paramDesc: () =>
            clack.text({
              message: 'Parameter description?',
              placeholder: 'Search query',
            }),
          paramRequired: () =>
            clack.confirm({
              message: 'Required?',
              initialValue: true,
            }),
          addMoreParams: () =>
            clack.confirm({
              message: 'Add another parameter?',
              initialValue: false,
            }),
        });

        if (clack.isCancel(paramAnswers)) break;

        const pName = paramAnswers.paramName as string;
        if (pName) {
          parameters.properties![pName] = {
            type: paramAnswers.paramType as 'string' | 'number' | 'boolean' | 'array',
            description: (paramAnswers.paramDesc as string) || undefined,
          };
          if (paramAnswers.paramRequired) {
            parameters.required!.push(pName);
          }
        }

        addMoreParams = paramAnswers.addMoreParams as boolean;
      }
    }

    tools.push({
      name: toolAnswers.name as string,
      description: (toolAnswers.description as string) || '',
      parameters,
    });

    const moreTools = await clack.confirm({
      message: 'Add another tool?',
      initialValue: false,
    });
    addMore = clack.isCancel(moreTools) ? false : (moreTools as boolean);
  }

  return tools;
}
```

- [ ] **Step 4: Implement create.ts command**

Create `packages/cli/src/commands/create.ts`:

```typescript
import { Command } from 'commander';
import { generateProject } from '@mcp-forge/core';
import { runCreatePrompts } from '../prompts/create-prompts.js';
import pc from 'picocolors';
import { execa } from 'execa';
import path from 'node:path';

export function registerCreateCommand(program: Command): void {
  program
    .command('create [name]')
    .description('Create a new MCP Server project')
    .option('-t, --template <template>', 'Template to use')
    .option('-d, --description <description>', 'Project description')
    .option('-a, --author <author>', 'Project author')
    .option('-y, --yes', 'Use defaults for all prompts')
    .action(async (name?: string, options?: { template?: string; description?: string; author?: string; yes?: boolean }) => {
      const answers = await runCreatePrompts({
        projectName: name,
        template: options?.template,
        description: options?.description,
        author: options?.author,
      });

      if (!answers) {
        process.exit(1);
      }

      const outputPath = path.resolve(process.cwd(), answers.projectName);

      console.log(pc.cyan(`\nCreating project ${pc.bold(answers.projectName)}...`));

      await generateProject({
        projectName: answers.projectName,
        description: answers.description,
        author: answers.author,
        template: answers.template,
        tools: answers.tools,
        outputPath,
      });

      // Install dependencies
      console.log(pc.cyan('Installing dependencies...'));
      try {
        await execa('npm', ['install'], { cwd: outputPath, stdio: 'inherit' });
      } catch {
        console.log(pc.yellow('Warning: npm install failed. Run it manually.'));
      }

      console.log(pc.green(`\nProject created at ${pc.bold(outputPath)}`));
      console.log(pc.dim('\nNext steps:'));
      console.log(pc.dim(`  cd ${answers.projectName}`));
      console.log(pc.dim('  npm run build'));
      console.log(pc.dim('  npm start'));
      console.log(pc.dim('\nTo inspect your server:'));
      console.log(pc.dim('  npm run inspect'));
    });
}
```

- [ ] **Step 5: Implement CLI entry point**

Create `packages/cli/src/index.ts`:

```typescript
import { Command } from 'commander';
import { registerCreateCommand } from './commands/create.js';

const program = new Command();

program
  .name('mcp-forge')
  .description('The fastest way to build MCP Servers')
  .version('0.1.0');

registerCreateCommand(program);

// Placeholder commands - will be implemented in later tasks
program
  .command('dev')
  .description('Start MCP Server in development mode with hot reload')
  .action(() => {
    console.log('mcp-forge dev - Coming soon!');
  });

program
  .command('test')
  .description('Run tests against your MCP Server')
  .action(() => {
    console.log('mcp-forge test - Coming soon!');
  });

program
  .command('inspect <path>')
  .description('Inspect a running MCP Server')
  .action(() => {
    console.log('mcp-forge inspect - Coming soon!');
  });

program
  .command('add <tool-name>')
  .description('Add a new tool to your MCP Forge project')
  .action(() => {
    console.log('mcp-forge add - Coming soon!');
  });

program.parse();
```

- [ ] **Step 6: Install dependencies and build**

Run: `cd /Users/javian/Downloads/trae && pnpm install && pnpm -r run build`

- [ ] **Step 7: Test the CLI manually**

Run: `cd /Users/javian/Downloads/trae && node packages/cli/dist/index.js --help`
Expected: Shows help with all commands listed

- [ ] **Step 8: Commit**

```bash
cd /Users/javian/Downloads/trae && git add packages/cli && git commit -m "feat(cli): add create command with interactive prompts"
```

---

### Task 6: Build CLI - Add Command

**Files:**
- Create: `packages/cli/src/commands/add.ts`
- Create: `packages/cli/src/prompts/add-prompts.ts`
- Modify: `packages/cli/src/index.ts`

- [ ] **Step 1: Implement add-prompts.ts**

Create `packages/cli/src/prompts/add-prompts.ts`:

```typescript
import * as clack from '@clack/prompts';
import type { ToolDefinition } from '@mcp-forge/core';

export async function runAddPrompts(toolName?: string): Promise<ToolDefinition | null> {
  const answers = await clack.group({
    name: () =>
      clack.text({
        message: 'Tool name?',
        placeholder: toolName || 'my-tool',
        defaultValue: toolName,
        validate: (v) => (!v.trim() ? 'Tool name is required' : undefined),
      }),
    description: () =>
      clack.text({
        message: 'Tool description?',
        placeholder: 'What does this tool do?',
      }),
    addParams: () =>
      clack.confirm({
        message: 'Add parameters?',
        initialValue: true,
      }),
  });

  if (clack.isCancel(answers)) {
    clack.cancel('Operation cancelled');
    return null;
  }

  const parameters: ToolDefinition['parameters'] = {
    type: 'object',
    properties: {},
    required: [],
  };

  if (answers.addParams) {
    let addMore = true;
    while (addMore) {
      const paramAnswers = await clack.group({
        paramName: () =>
          clack.text({ message: 'Parameter name?', placeholder: 'query' }),
        paramType: () =>
          clack.select({
            message: 'Parameter type?',
            options: [
              { value: 'string', label: 'string' },
              { value: 'number', label: 'number' },
              { value: 'boolean', label: 'boolean' },
              { value: 'array', label: 'array' },
            ],
          }),
        paramDesc: () =>
          clack.text({ message: 'Parameter description?' }),
        paramRequired: () =>
          clack.confirm({ message: 'Required?', initialValue: true }),
        addMore: () =>
          clack.confirm({ message: 'Add another parameter?', initialValue: false }),
      });

      if (clack.isCancel(paramAnswers)) break;

      const pName = paramAnswers.paramName as string;
      if (pName) {
        parameters.properties![pName] = {
          type: paramAnswers.paramType as 'string' | 'number' | 'boolean' | 'array',
          description: (paramAnswers.paramDesc as string) || undefined,
        };
        if (paramAnswers.paramRequired) {
          parameters.required!.push(pName);
        }
      }

      addMore = paramAnswers.addMore as boolean;
    }
  }

  return {
    name: answers.name as string,
    description: (answers.description as string) || '',
    parameters,
  };
}
```

- [ ] **Step 2: Implement add.ts command**

Create `packages/cli/src/commands/add.ts`:

```typescript
import { Command } from 'commander';
import { runAddPrompts } from '../prompts/add-prompts.js';
import pc from 'picocolors';
import { readFile, writeFile, mkdir } from 'node:fs/promises';
import { join, dirname } from 'node:path';
import { renderTemplate } from '@mcp-forge/core';
import type { ToolDefinition } from '@mcp-forge/core';

const TOOL_TEMPLATE = `import { z } from 'zod/v3';

export function register{{pascalCase name}}(server: import('@modelcontextprotocol/sdk/server/mcp.js').McpServer) {
  server.tool(
    '{{name}}',
    '{{description}}',
    {
      {{#each parameters.properties}}
      {{@key}}: z.{{zodType this}}(){{#if this.description}}.describe('{{this.description}}'){{/if}},
      {{/each}}
    },
    async ({ {{parameterList}} }) => {
      // TODO: Implement {{name}} tool logic
      return {
        content: [{ type: 'text', text: JSON.stringify({ result: '{{name}} executed' }) }],
      };
    }
  );
}
`;

export function registerAddCommand(program: Command): void {
  program
    .command('add [tool-name]')
    .description('Add a new tool to your MCP Forge project')
    .action(async (toolName?: string) => {
      const toolDef = await runAddPrompts(toolName);

      if (!toolDef) {
        process.exit(1);
      }

      const projectPath = process.cwd();

      // Generate tool file
      const paramNames = Object.keys(toolDef.parameters.properties || {});
      const templateData = {
        ...toolDef,
        parameterList: paramNames.join(', '),
        parameters: {
          ...toolDef.parameters,
          properties: Object.fromEntries(
            Object.entries(toolDef.parameters.properties || {}).map(([key, val]) => [
              key,
              { ...val, zodType: zodTypeFromSchema(val as { type: string }) },
            ])
          ),
        },
      };

      const content = renderTemplate(TOOL_TEMPLATE, templateData);
      const toolPath = join(projectPath, 'src', 'tools', `${toolDef.name}.ts`);

      await mkdir(dirname(toolPath), { recursive: true });
      await writeFile(toolPath, content, 'utf-8');

      // Update index.ts to import and register the new tool
      const indexPath = join(projectPath, 'src', 'index.ts');
      try {
        let indexContent = await readFile(indexPath, 'utf-8');

        // Add import
        const importLine = `import { register${toPascalCase(toolDef.name)} } from './tools/${toolDef.name}.js';`;
        if (!indexContent.includes(importLine)) {
          // Insert after the last import
          const lastImportIndex = indexContent.lastIndexOf('import ');
          const lineEnd = indexContent.indexOf('\n', lastImportIndex);
          indexContent =
            indexContent.slice(0, lineEnd + 1) +
            importLine +
            '\n' +
            indexContent.slice(lineEnd + 1);
        }

        // Add registration
        const regLine = `register${toPascalCase(toolDef.name)}(server);`;
        if (!indexContent.includes(regLine)) {
          // Insert before the main function
          const mainIndex = indexContent.indexOf('async function main()');
          if (mainIndex !== -1) {
            indexContent =
              indexContent.slice(0, mainIndex) +
              regLine +
              '\n\n' +
              indexContent.slice(mainIndex);
          }
        }

        await writeFile(indexPath, indexContent, 'utf-8');
      } catch {
        console.log(pc.yellow('Warning: Could not update index.ts. Add the import manually.'));
      }

      console.log(pc.green(`\nTool ${pc.bold(toolDef.name)} added!`));
      console.log(pc.dim(`  Created: src/tools/${toolDef.name}.ts`));
      console.log(pc.dim(`  Updated: src/index.ts`));
    });
}

function toPascalCase(str: string): string {
  return str
    .replace(/(^|[-_\s])(\w)/g, (_match, _sep, char) => char.toUpperCase())
    .replace(/[-_\s]/g, '');
}

function zodTypeFromSchema(param: { type: string }): string {
  const typeMap: Record<string, string> = {
    string: 'string',
    number: 'number',
    boolean: 'boolean',
    array: 'array',
    object: 'object',
  };
  return typeMap[param.type] || 'string';
}
```

- [ ] **Step 3: Update index.ts to register add command**

Modify `packages/cli/src/index.ts` - add import and register:

```typescript
import { Command } from 'commander';
import { registerCreateCommand } from './commands/create.js';
import { registerAddCommand } from './commands/add.js';

const program = new Command();

program
  .name('mcp-forge')
  .description('The fastest way to build MCP Servers')
  .version('0.1.0');

registerCreateCommand(program);
registerAddCommand(program);

// Placeholder commands
program
  .command('dev')
  .description('Start MCP Server in development mode with hot reload')
  .action(() => {
    console.log('mcp-forge dev - Coming soon!');
  });

program
  .command('test')
  .description('Run tests against your MCP Server')
  .action(() => {
    console.log('mcp-forge test - Coming soon!');
  });

program
  .command('inspect <path>')
  .description('Inspect a running MCP Server')
  .action(() => {
    console.log('mcp-forge inspect - Coming soon!');
  });

program.parse();
```

- [ ] **Step 4: Build and test**

Run: `cd /Users/javian/Downloads/trae && pnpm -r run build && node packages/cli/dist/index.js add --help`

- [ ] **Step 5: Commit**

```bash
cd /Users/javian/Downloads/trae && git add packages/cli && git commit -m "feat(cli): add 'add' command for adding tools to existing projects"
```

---

### Task 7: Build Dev Server Package

**Files:**
- Create: `packages/dev-server/package.json`
- Create: `packages/dev-server/tsconfig.json`
- Create: `packages/dev-server/tsup.config.ts`
- Create: `packages/dev-server/src/index.ts`
- Create: `packages/dev-server/src/watcher.ts`
- Create: `packages/dev-server/src/runner.ts`
- Create: `packages/dev-server/src/tui/index.ts`
- Create: `packages/dev-server/src/tui/app.tsx`
- Create: `packages/dev-server/src/tui/log-panel.tsx`
- Modify: `packages/cli/src/commands/dev.ts` (create new file)
- Modify: `packages/cli/src/index.ts`

- [ ] **Step 1: Create packages/dev-server/package.json**

```json
{
  "name": "@mcp-forge/dev-server",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@mcp-forge/core": "workspace:*",
    "ink": "^5.0.0",
    "react": "^18.3.0",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
```

- [ ] **Step 2: Implement watcher.ts**

Create `packages/dev-server/src/watcher.ts`:

```typescript
import { watch } from 'chokidar';
import type { FSWatcher } from 'chokidar';

export interface WatcherOptions {
  paths: string[];
  onChange: (path: string) => void;
  onReady?: () => void;
}

export function createWatcher(options: WatcherOptions): FSWatcher {
  const watcher = watch(options.paths, {
    ignored: ['**/node_modules/**', '**/dist/**', '**/.git/**'],
    persistent: true,
    ignoreInitial: true,
  });

  watcher.on('change', (path) => {
    options.onChange(path);
  });

  watcher.on('ready', () => {
    options.onReady?.();
  });

  return watcher;
}
```

- [ ] **Step 3: Implement runner.ts**

Create `packages/dev-server/src/runner.ts`:

```typescript
import { spawn, type ChildProcess } from 'node:child_process';
import { resolve } from 'node:path';

export interface ServerRunnerOptions {
  projectPath: string;
  onLog: (message: string) => void;
  onError: (message: string) => void;
  onExit: (code: number | null) => void;
}

export class ServerRunner {
  private process: ChildProcess | null = null;
  private options: ServerRunnerOptions;

  constructor(options: ServerRunnerOptions) {
    this.options = options;
  }

  async start(): Promise<void> {
    this.stop();

    const entryPoint = resolve(this.options.projectPath, 'dist', 'index.js');

    this.process = spawn('node', [entryPoint], {
      cwd: this.options.projectPath,
      stdio: ['pipe', 'pipe', 'pipe'],
      env: { ...process.env },
    });

    this.process.stdout?.on('data', (data: Buffer) => {
      this.options.onLog(data.toString().trim());
    });

    this.process.stderr?.on('data', (data: Buffer) => {
      this.options.onError(data.toString().trim());
    });

    this.process.on('exit', (code) => {
      this.options.onExit(code);
    });
  }

  stop(): void {
    if (this.process && !this.process.killed) {
      this.process.kill('SIGTERM');
      this.process = null;
    }
  }

  isRunning(): boolean {
    return this.process !== null && !this.process.killed;
  }
}
```

- [ ] **Step 4: Implement TUI app**

Create `packages/dev-server/src/tui/app.tsx`:

```tsx
import React, { useState, useEffect } from 'react';
import { Box, Text, useApp, useInput } from 'ink';
import { LogPanel } from './log-panel.js';

export interface LogEntry {
  timestamp: Date;
  type: 'info' | 'error' | 'tool-call' | 'tool-result';
  message: string;
}

export interface DevAppProps {
  projectName: string;
  onLog: (entry: LogEntry) => void;
  logs: LogEntry[];
  serverStatus: 'starting' | 'running' | 'stopped' | 'error';
  onRestart: () => void;
}

export function DevApp({ projectName, logs, serverStatus, onRestart }: DevAppProps) {
  const { exit } = useApp();

  useInput((input) => {
    if (input === 'q') {
      exit();
    }
    if (input === 'r') {
      onRestart();
    }
  });

  const statusColor = {
    starting: 'yellow',
    running: 'green',
    stopped: 'gray',
    error: 'red',
  }[serverStatus];

  return (
    <Box flexDirection="column" padding={1}>
      <Box marginBottom={1}>
        <Text bold color="cyan">
          MCP Forge Dev
        </Text>
        <Text> | </Text>
        <Text color="white">{projectName}</Text>
        <Text> | </Text>
        <Text color={statusColor}>
          {serverStatus.toUpperCase()}
        </Text>
      </Box>

      <Box flexDirection="column" marginBottom={1}>
        <Text bold>Logs:</Text>
        <LogPanel logs={logs} />
      </Box>

      <Box>
        <Text dimColor>
          [r] Restart | [q] Quit
        </Text>
      </Box>
    </Box>
  );
}
```

Create `packages/dev-server/src/tui/log-panel.tsx`:

```tsx
import React from 'react';
import { Box, Text } from 'ink';
import type { LogEntry } from './app.js';

export interface LogPanelProps {
  logs: LogEntry[];
  maxLines?: number;
}

export function LogPanel({ logs, maxLines = 20 }: LogPanelProps) {
  const visibleLogs = logs.slice(-maxLines);

  return (
    <Box flexDirection="column">
      {visibleLogs.map((log, i) => (
        <Box key={i}>
          <Text dimColor>
            [{log.timestamp.toLocaleTimeString()}]
          </Text>
          <Text
            color={
              log.type === 'error' ? 'red' :
              log.type === 'tool-call' ? 'cyan' :
              log.type === 'tool-result' ? 'green' :
              'white'
            }
          >
            {' '}
            {log.message}
          </Text>
        </Box>
      ))}
    </Box>
  );
}
```

Create `packages/dev-server/src/tui/index.ts`:

```typescript
export { DevApp } from './app.js';
export type { LogEntry, DevAppProps } from './app.js';
```

- [ ] **Step 5: Implement dev-server index.ts**

Create `packages/dev-server/src/index.ts`:

```typescript
import React from 'react';
import { render } from 'ink';
import { createWatcher, type WatcherOptions } from './watcher.js';
import { ServerRunner } from './runner.js';
import { DevApp, type LogEntry } from './tui/index.js';

export interface DevServerOptions {
  projectPath: string;
  watchPaths?: string[];
}

export async function startDevServer(options: DevServerOptions): Promise<void> {
  const { projectPath } = options;
  const watchPaths = options.watchPaths || ['./src'];
  const logs: LogEntry[] = [];
  let serverStatus: 'starting' | 'running' | 'stopped' | 'error' = 'stopped';

  const runner = new ServerRunner({
    projectPath,
    onLog: (message) => {
      logs.push({ timestamp: new Date(), type: 'info', message });
    },
    onError: (message) => {
      logs.push({ timestamp: new Date(), type: 'error', message });
    },
    onExit: (code) => {
      serverStatus = code === 0 ? 'stopped' : 'error';
    },
  });

  async function startServer() {
    serverStatus = 'starting';
    logs.push({ timestamp: new Date(), type: 'info', message: 'Building project...' });

    try {
      const { execa } = await import('execa');
      await execa('npm', ['run', 'build'], { cwd: projectPath, stdio: 'pipe' });
      logs.push({ timestamp: new Date(), type: 'info', message: 'Build complete. Starting server...' });
      await runner.start();
      serverStatus = 'running';
    } catch (error) {
      serverStatus = 'error';
      logs.push({
        timestamp: new Date(),
        type: 'error',
        message: `Build failed: ${error instanceof Error ? error.message : String(error)}`,
      });
    }
  }

  // Start server initially
  await startServer();

  // Set up file watcher
  const watcher = createWatcher({
    paths: watchPaths.map((p) => `${projectPath}/${p}`),
    onChange: async (path) => {
      logs.push({ timestamp: new Date(), type: 'info', message: `File changed: ${path}. Rebuilding...` });
      runner.stop();
      await startServer();
    },
    onReady: () => {
      logs.push({ timestamp: new Date(), type: 'info', message: 'Watching for changes...' });
    },
  });

  // Render TUI
  const { waitUntilExit } = render(
    React.createElement(DevApp, {
      projectName: projectPath.split('/').pop() || 'mcp-server',
      logs,
      serverStatus,
      onRestart: async () => {
        runner.stop();
        await startServer();
      },
      onLog: (entry: LogEntry) => {
        logs.push(entry);
      },
    })
  );

  await waitUntilExit();

  // Cleanup
  runner.stop();
  await watcher.close();
}

export { createWatcher, ServerRunner };
export type { DevServerOptions, LogEntry };
```

- [ ] **Step 6: Implement dev.ts command**

Create `packages/cli/src/commands/dev.ts`:

```typescript
import { Command } from 'commander';
import { startDevServer } from '@mcp-forge/dev-server';
import { resolve } from 'node:path';

export function registerDevCommand(program: Command): void {
  program
    .command('dev')
    .description('Start MCP Server in development mode with hot reload')
    .option('-p, --project <path>', 'Project path', '.')
    .option('-w, --watch <paths>', 'Comma-separated watch paths', './src')
    .action(async (options: { project: string; watch: string }) => {
      const projectPath = resolve(options.project);
      const watchPaths = options.watch.split(',').map((p: string) => p.trim());

      await startDevServer({ projectPath, watchPaths });
    });
}
```

- [ ] **Step 7: Update CLI index.ts to register dev command**

Replace the placeholder dev command in `packages/cli/src/index.ts` with:

```typescript
import { Command } from 'commander';
import { registerCreateCommand } from './commands/create.js';
import { registerAddCommand } from './commands/add.js';
import { registerDevCommand } from './commands/dev.js';

const program = new Command();

program
  .name('mcp-forge')
  .description('The fastest way to build MCP Servers')
  .version('0.1.0');

registerCreateCommand(program);
registerAddCommand(program);
registerDevCommand(program);

// Placeholder commands
program
  .command('test')
  .description('Run tests against your MCP Server')
  .action(() => {
    console.log('mcp-forge test - Coming soon!');
  });

program
  .command('inspect <path>')
  .description('Inspect a running MCP Server')
  .action(() => {
    console.log('mcp-forge inspect - Coming soon!');
  });

program.parse();
```

- [ ] **Step 8: Build and commit**

Run: `cd /Users/javian/Downloads/trae && pnpm install && pnpm -r run build`

```bash
cd /Users/javian/Downloads/trae && git add packages/dev-server packages/cli && git commit -m "feat(dev-server): add hot-reload dev mode with TUI"
```

---

### Task 8: Build CLI - Inspect Command

**Files:**
- Create: `packages/cli/src/commands/inspect.ts`
- Modify: `packages/cli/src/index.ts`

- [ ] **Step 1: Implement inspect.ts**

Create `packages/cli/src/commands/inspect.ts`:

```typescript
import { Command } from 'commander';
import { Client } from '@modelcontextprotocol/sdk/client/index.js';
import { StdioClientTransport } from '@modelcontextprotocol/sdk/client/stdio.js';
import pc from 'picocolors';
import { spawn } from 'node:child_process';
import { resolve } from 'node:path';

export function registerInspectCommand(program: Command): void {
  program
    .command('inspect <path>')
    .description('Inspect a running MCP Server or start one and inspect it')
    .option('-a, --args <args>', 'Additional arguments to pass to the server', '')
    .action(async (serverPath: string, options: { args?: string }) => {
      const absolutePath = resolve(serverPath);

      console.log(pc.cyan(`Inspecting MCP Server: ${pc.bold(absolutePath)}\n`));

      try {
        const transport = new StdioClientTransport({
          command: 'node',
          args: [absolutePath, ...(options.args ? options.args.split(' ') : [])],
        });

        const client = new Client({
          name: 'mcp-forge-inspector',
          version: '0.1.0',
        });

        await client.connect(transport);

        // List tools
        console.log(pc.bold(pc.cyan('Tools:')));
        const toolsResult = await client.listTools();
        if (toolsResult.tools.length === 0) {
          console.log(pc.dim('  No tools registered'));
        } else {
          for (const tool of toolsResult.tools) {
            console.log(`  ${pc.green(tool.name)} - ${tool.description || pc.dim('No description')}`);
            if (tool.inputSchema && typeof tool.inputSchema === 'object' && 'properties' in tool.inputSchema) {
              const props = (tool.inputSchema as { properties: Record<string, unknown> }).properties;
              for (const [key, value] of Object.entries(props)) {
                const v = value as { type?: string; description?: string };
                console.log(pc.dim(`    ${key}: ${v.type || 'any'}${v.description ? ` - ${v.description}` : ''}`));
              }
            }
          }
        }

        // List resources
        console.log(pc.bold(pc.cyan('\nResources:')));
        try {
          const resourcesResult = await client.listResources();
          if (resourcesResult.resources.length === 0) {
            console.log(pc.dim('  No resources registered'));
          } else {
            for (const resource of resourcesResult.resources) {
              console.log(`  ${pc.green(resource.name)} - ${resource.uri}`);
            }
          }
        } catch {
          console.log(pc.dim('  Resources not supported'));
        }

        // List prompts
        console.log(pc.bold(pc.cyan('\nPrompts:')));
        try {
          const promptsResult = await client.listPrompts();
          if (promptsResult.prompts.length === 0) {
            console.log(pc.dim('  No prompts registered'));
          } else {
            for (const prompt of promptsResult.prompts) {
              console.log(`  ${pc.green(prompt.name)} - ${prompt.description || pc.dim('No description')}`);
            }
          }
        } catch {
          console.log(pc.dim('  Prompts not supported'));
        }

        await client.close();
        console.log(pc.dim('\nInspection complete.'));
      } catch (error) {
        console.error(pc.red(`Error: ${error instanceof Error ? error.message : String(error)}`));
        process.exit(1);
      }
    });
}
```

- [ ] **Step 2: Update CLI index.ts**

Replace the placeholder inspect command with the real one:

```typescript
import { Command } from 'commander';
import { registerCreateCommand } from './commands/create.js';
import { registerAddCommand } from './commands/add.js';
import { registerDevCommand } from './commands/dev.js';
import { registerInspectCommand } from './commands/inspect.js';

const program = new Command();

program
  .name('mcp-forge')
  .description('The fastest way to build MCP Servers')
  .version('0.1.0');

registerCreateCommand(program);
registerAddCommand(program);
registerDevCommand(program);
registerInspectCommand(program);

// Placeholder
program
  .command('test')
  .description('Run tests against your MCP Server')
  .action(() => {
    console.log('mcp-forge test - Coming soon!');
  });

program.parse();
```

- [ ] **Step 3: Build and commit**

Run: `cd /Users/javian/Downloads/trae && pnpm -r run build`

```bash
cd /Users/javian/Downloads/trae && git add packages/cli && git commit -m "feat(cli): add inspect command for MCP Server introspection"
```

---

### Task 9: Add Root README and Final Polish

**Files:**
- Create: `README.md`
- Modify: `packages/cli/package.json` (add keywords, repository)

- [ ] **Step 1: Create README.md**

Create `README.md`:

```markdown
# MCP Forge

> The fastest way to build MCP Servers

MCP Forge is a CLI tool that scaffolds, develops, and inspects [MCP (Model Context Protocol)](https://modelcontextprotocol.io/) servers. Create a production-ready MCP server in 30 seconds.

## Features

- **`mcp-forge create`** - Interactive scaffold with 6 built-in templates
- **`mcp-forge add`** - Add new tools to existing projects
- **`mcp-forge dev`** - Hot-reload development mode with TUI
- **`mcp-forge inspect`** - Inspect any MCP Server's capabilities

## Quick Start

```bash
# Create a new MCP Server (no install needed)
npx @mcp-forge/cli create my-server

# Or install globally
npm install -g @mcp-forge/cli
mcp-forge create my-server
```

## Templates

| Template | Description |
|----------|-------------|
| `minimal` | Minimal MCP server with echo tool |
| `rest-api` | REST API wrapper |
| `database` | Database operations (SQLite) |
| `filesystem` | File system operations |
| `web-scraper` | Web scraping with Cheerio |
| `ai-tool` | AI/LLM API wrapper (OpenAI) |

## Usage

### Create a project

```bash
mcp-forge create my-weather-server --template rest-api
```

### Add a tool

```bash
cd my-weather-server
mcp-forge add get-forecast
```

### Develop with hot reload

```bash
mcp-forge dev
```

### Inspect a server

```bash
mcp-forge inspect ./dist/index.js
```

## Generated Project

A generated MCP Forge project looks like:

```
my-server/
├── src/
│   ├── index.ts      # MCP Server entry
│   └── tools/        # Tool implementations
├── tests/            # Test files
├── package.json
├── tsconfig.json
├── mcp-forge.config.ts
└── README.md
```

## Use with Claude Desktop

Add to `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "my-server": {
      "command": "node",
      "args": ["/path/to/my-server/dist/index.js"]
    }
  }
}
```

## License

MIT
```

- [ ] **Step 2: Commit**

```bash
cd /Users/javian/Downloads/trae && git add README.md && git commit -m "docs: add project README"
```

---

### Task 10: End-to-End Test

**Files:** None (manual verification)

- [ ] **Step 1: Build all packages**

Run: `cd /Users/javian/Downloads/trae && pnpm install && pnpm -r run build`

- [ ] **Step 2: Test create command**

Run: `cd /tmp && node /Users/javian/Downloads/trae/packages/cli/dist/index.js create test-server --template minimal --yes`
Expected: Project created successfully with all files

- [ ] **Step 3: Test inspect command**

Run: `cd /tmp/test-server && npm install && npm run build && node /Users/javian/Downloads/trae/packages/cli/dist/index.js inspect ./dist/index.js`
Expected: Shows echo tool with its parameters

- [ ] **Step 4: Final commit**

```bash
cd /Users/javian/Downloads/trae && git add -A && git commit -m "chore: final polish and e2e verification"
```
